# Legacy frontend (pre C1 UI overhaul)

Snapshot of `frontend/src/` before applying the Together (C1) design language.

Use this folder to compare or revert UI changes from the overhaul described in `misc/design/implement.md`.

To run the old UI locally, temporarily point Vite at files in this directory or restore selected files from here.
